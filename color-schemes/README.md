# Color schemes

Place the finished KDE `.colors` files here, one for Xef Dark and one for Xef Light.

Once their internal `Name`/scheme identifiers are known, add the corresponding `ColorScheme=` entry to each Global Theme's `contents/defaults` file.

# Cursors

Both variants reference the external `Hackneyed-Dark` cursor theme found in the live configuration.

It is not vendored. The installer detects it in both modern XDG icon paths and the legacy `~/.icons` path; if missing, it downloads the pinned 0.9.3 scalable right-handed release through KDE Store and installs it to the modern user-local XDG path.
